<features name="${project.artifactId}-${project.version}"
          xmlns="http://karaf.apache.org/xmlns/features/v1.6.0">

  <feature description='${project.name}' name='${project.artifactId}' start-level="100"
           version='${project.version}'>

    <!-- Undertow is no longer pinned here: pax-web 8.0.35 ships 2.2.39.Final itself,
         which is what the former HOTFIX pin was waiting for. -->
  </feature>


</features>